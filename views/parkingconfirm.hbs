<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parking Booking</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        * {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            outline: none;
            border: none;
            text-transform: capitalize;
            transition: all .2s linear;
        }

        body {
            background-image: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url("/parking-background.jpg");
            background-size:cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }




        .header-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background: rgba(255, 255, 255, 0.5); /* Updated background color */
        }

        .header-row h2 {
            font-size: 24px;
            color: #333;
        }

        #countdown {
            font-size: 18px;
            color: #333;
        }

        .booking-form {
            padding: 20px;
            width: 60%; /* Adjusted width to make it a little smaller */
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.8); /* Updated background to white with transparency */
            box-shadow: 0 5px 10px rgba(0, 0, 0, .1);
        }

        .booking-form label {
            font-size: 15px;
            color: #333;
            text-transform: uppercase;
        }

        .booking-form input {
            width: 100%;
            border: 1px solid #ccc;
            padding: 10px 15px;
            font-size: 15px;
            text-transform: none;
            background: rgba(255, 255, 255, 0.5); /* Updated input background to white with transparency */
        }

        .booking-form input:focus {
            border: 1px solid #000;
        }

        .booking-form button {
            width: 100%;
            padding: 12px;
            font-size: 17px;
            background: #007BFF; /* Changed button color to blue */
            color: #fff;
            margin-top: 15px;
            cursor: pointer;
        }

        .booking-form button:hover {
            background: #0056b3; /* Darker blue on hover */
        }

        .payment-method-section {
            display: none;
            padding: 20px;
            width: 60%; /* Adjusted width to make it a little smaller */
            margin: 20px auto;
            background: rgba(255, 255, 255, 0.8); /* Updated background to white with transparency */
            box-shadow: 0 5px 10px rgba(0, 0, 0, .1);
        }

        .payment-method-section h2 {
            font-size: 20px;
            color: #333;
        }

        .payment-buttons {
            display: flex;
            gap: 10px;
        }

        .payment-btn {
            background-color: #007BFF; /* Changed button color to blue */
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .payment-btn:hover {
             background-color: #0056b3; /* Darker blue on hover */
        }

        .payment-btn.active {
            background-color: #0056b3; /* Darker blue for active state */
        }       

        .inputBox {
            margin: 15px 0;
        }

        .inputBox span {
            margin-bottom: 10px;
            display: block;
            font-size: 15px;
            color: #333;
        }

        .inputBox input {
            width: 100%;
            border: 1px solid #ccc;
            padding: 10px 15px;
            font-size: 15px;
            text-transform: none;
        }

        .inputBox input:focus {
            border: 1px solid #000;
        }

        .submit-btn {
            width: 100%;
            padding: 12px;
            font-size: 17px;
            background: #056114;
            color: #fff;
            margin-top: 5px;
            cursor: pointer;
        }

        .submit-btn:hover {
            background: #1fa335;
        }
    </style>
</head>

<body>
    <!-- Header Row -->
    <div class="header-row">
        <h2>Booking Information</h2>
        <div id="countdown">Time remaining: <span id="timer">120</span> seconds</div>
    </div>

    <!-- Booking Form -->
    <div class="booking-form">
        <form id="bookingForm">
            <label for="fullname">Full Name:</label>
            <input type="text" id="fullname" required>

            <label for="phone">Phone Number:</label>
            <input type="tel" id="phone" required>

            <label for="licensePlate">License Plate:</label>
            <input type="text" id="licensePlate" required>

            <label for="studentId">Student ID:</label>
            <input type="text" id="studentId" required>

            <button type="button" id="confirmBtn" onclick="showPaymentSection()">Confirm Booking</button>
        </form>
    </div>

    <!-- Payment Method Section (Initially Hidden) -->
    <div class="payment-method-section" id="paymentSection" style="display: none;">
        <h2>Select Payment Method</h2>
        <div class="payment-buttons">
            <button type="button" class="payment-btn" id="bkashBtn" onclick="selectPayment('bkash')">bKash</button>
            <button type="button" class="payment-btn" id="nagadBtn" onclick="selectPayment('nagad')">Nagad</button>
        </div>
        <div class="inputBox">
            <span>Transaction ID :</span>
            <input type="text" placeholder="Enter Transaction ID" id="transactionInput">
        </div>
        <button type="submit" value="Proceed to Checkout" class="submit-btn">Proceed to Checkout</button>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="handlebars-v4.7.7.js"></script>
    <script src="app.js"></script>

    <script>
        function showPaymentSection() {
            // Get the payment method section element
            var paymentSection = document.getElementById("paymentSection");

            // Set the display property to "block" to make it visible
            paymentSection.style.display = "block";
        }
    </script>
</body>

</html>